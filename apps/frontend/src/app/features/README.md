# Features

Feature modules live here. Each feature should own its pages, components, hooks, services, and types.

Suggested structure:

```
features/<feature>/
  components/
  hooks/
  pages/
  services/
  types/
  index.ts
```

Keep feature modules self-contained and import from here directly (no legacy paths).
