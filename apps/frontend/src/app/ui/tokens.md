# UI Tokens

Primary source of tokens is `src/app/globals.css` under the `@theme` block.

Notes:
- Use `--color-*` tokens for UI colors.
- Prefer `--font-satoshi` and `--font-grotesk` for typography.
- Token cleanup should preserve current effective values.
