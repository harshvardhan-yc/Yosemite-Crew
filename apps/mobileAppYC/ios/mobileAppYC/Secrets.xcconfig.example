// Copy this file to Secrets.xcconfig in the same directory.
// Secrets.xcconfig is gitignored — never commit it.
//
// Get your key from Google Cloud Console → APIs & Services → Credentials.
// Enable: Maps SDK for iOS (and optionally Places API) for this key.

GOOGLE_MAPS_API_KEY = your_ios_google_maps_api_key_here
